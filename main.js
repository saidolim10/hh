let names = prompt(`Ismingizmni kriting`)
let id = prompt(`space idingizmni kriting`)
let parol = prompt(`space parolingizmni kriting`)

let obj = {
    name: names,
    id: id,
    password: parol
}

console.log(obj);